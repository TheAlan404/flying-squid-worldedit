var prefix = "[WorldEdit] "
console.log(prefix + "Plugin enabled.");
let thereBe = "light";

module.exports.serv = (serv) => {
  
};

module.exports.player = (player, serv) => {
  let pos1;
  let pos2;
  
  
};
